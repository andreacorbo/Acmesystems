# root crontab
# DO NOT EDIT THIS FILE MANUALLY! USE crontab -e INSTEAD

# man 1 crontab for acceptable formats:
#    <minute> <hour> <day> <month> <dow> <tags and command>
#    <@freq> <tags and command>

# SYSTEM DAILY/WEEKLY/... FOLDERS
@hourly         ID=sys-hourly   /bin/run-parts /etc/cron.hourly
@daily          ID=sys-daily    /bin/run-parts /etc/cron.daily
@weekly         ID=sys-weekly   /bin/run-parts /etc/cron.weekly
@monthly        ID=sys-monthly  /bin/run-parts /etc/cron.monthly

#
# Reboot.
#
15 0 * * * reboot

#
# Check for wifi flag.
#
*/5 * * * * python /root/scripts/wifi.py

#
# Capture images.               
#
#4-59/10 * * * * sh /root/scripts/capture.sh

#
# Send data and receive updates.
#
5 * * * * python /root/scripts/iridium.py --ota

